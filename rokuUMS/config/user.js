const sqlCreds = {
    connectionLimit : 10,
    host            : 'localhost',
    user            : 'root',
    password        : 'root',
    database        : 'roku_users',
    port            : 8889
}

module.exports = sqlCreds;